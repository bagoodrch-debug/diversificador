name: Atualizar cotações

on:
  schedule:
    # Uma vez por hora, no minuto 0. As fontes de dados (Yahoo Finance,
    # AwesomeAPI, sandbox da Brapi) não têm token nem cota paga, mas também
    # não têm garantia contratual — manter isso moderado evita parecer abuso.
    - cron: "0 * * * *"
  workflow_dispatch: {} # permite rodar manualmente pela aba Actions
  push:
    branches: [main]
    paths:
      - "carteira/scripts/atualizar-cotacoes.mjs"
      - ".github/workflows/atualizar-cotacoes.yml"

permissions:
  contents: write

jobs:
  atualizar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Buscar cotações e gerar dados/cotacoes.json
        working-directory: carteira
        run: node scripts/atualizar-cotacoes.mjs

      - name: Publicar dados atualizados
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add carteira/dados/cotacoes.json
          git diff --staged --quiet && echo "Sem mudanças." || git commit -m "chore: atualizar cotações [skip ci]"
          git push
